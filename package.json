{
  "name": "a11y-lens",
  "private": true,
  "packageManager": "pnpm@10.9.0",
  "scripts": {
    "dev": "turbo run dev panda:watch",
    "build": "turbo run build",
    "build:extension": "pnpm --filter a11y-lens-extension build"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.14"
  }
}
